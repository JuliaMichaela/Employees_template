import React from 'react'
import { connect } from 'react-redux'

export const Employeer = (props) => {
    return (
        <div>Employeer</div>
    )
}

const mapStateToProps = (state) => ({})

const mapDispatchToProps = {}

export default connect(mapStateToProps, mapDispatchToProps)(Employeer)